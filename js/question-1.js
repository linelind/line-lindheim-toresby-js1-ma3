//writing this function
/* 
function getRemainder(a, b) {
    return a % b;
}
 */
//as an arrow function
const getRemainder = (a, b) => a % b;

//and calling it to see that the arrow function works.
/* 
const result = getRemainder(9, 4);
console.log(result); 
*/